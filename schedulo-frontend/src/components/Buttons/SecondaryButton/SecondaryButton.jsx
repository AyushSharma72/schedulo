const SecondaryButton = ({ text }) => {
  return (
    <button
      type="submit"
      className="btn-secondary"
    >
      {text}
      {/* Sign In */}
    </button>
  );
};

export default SecondaryButton;
